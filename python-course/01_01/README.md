# 計算の復習
## Point
同じ数字を用いた累積代入の計算を複数行う場合は毎度計算の前に値を入力し直す必要がある
## Learn
累積代入（x+=y）は計算に同じ関数を何度も用いない場合や、計算をリセットせずに継ぎ足したいときに便利
普通の計算式（a=x+y）は何度も同じ数字を用いるときに便利
「ｎの数字になるまで」といったfor文やwhile文では累積代入の方が適していそう
## KeyWord
print,累積代入
